{
  "mcpServers": {
    "gtm-intelligence": {
      "command": "python",
      "args": ["code/mcp_server.py"],
      "env": {
        "CLAUDE_API_KEY": "${CLAUDE_API_KEY}",
        "DATABASE_URL": "${DATABASE_URL}",
        "REDIS_URL": "${REDIS_URL}",
        "FILE_STORE_PATH": "./briefs"
      }
    }
  }
}
